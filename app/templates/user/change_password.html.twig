{% include 'default/_form.html.twig' with {
    page_title: 'title.changePasswordByAdmin'|trans({'%id%': user.id|default(''), '%username%': user.username|default('')}),
    form: form,
    submit_label: 'action.changePassword'|trans,
    back_to_list_path: 'user_index',
} %}
