{% include 'default/_form.html.twig' with {
    page_title: 'title.userUnblock'|trans({'%id%': user.id|default(''), '%username%': user.username|default('')}),
    form: form,
    submit_label: 'action.unblock'|trans,
    back_to_list_path: 'user_index',
} %}
